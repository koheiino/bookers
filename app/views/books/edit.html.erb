<div class="container">
<% if @book.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@book.errors.count, "error") %> prohibited this record from being saved:</h2>

    <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>


  <h1>編集画面</h1>
  <%= form_with model: @book, url: book_path(@book), method: :patch do |f| %>
    <h4>タイトル</h4>
    <%= f.text_field :title %>
  
    <h4>本文</h4>
    <%= f.text_area :body %>
  
    <%= f.submit 'Update Book' %>
  
  <% end %>
  <div class="right-layout index-section">
    <p><%= link_to 'Show', book_path(@book) %></p>
    <p>|</p>
    <p><%= link_to 'Back', books_path %></p>
  </div>
</div>